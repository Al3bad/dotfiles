# Execute this block of on Arch Linux
if [[ ! -z "$(command -v hostnamectl)" && "$(hostnamectl | grep "Operating System:" | cut -d ':' -f2 | tr -d " ")" == "ArchLinux" ]]; then
    if [[ "$(tty)" = "/dev/tty1" ]]; then
        startx "~/.xinitrc"
    fi
fi
